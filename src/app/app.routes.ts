import { Routes } from '@angular/router';
import { CrudprojectngComponent } from './crudprojectng/crudprojectng.component';

export const routes: Routes = [
  { path: '', component: CrudprojectngComponent }
];